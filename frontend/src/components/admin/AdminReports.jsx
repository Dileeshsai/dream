import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const AdminReports = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Reports</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the placeholder for the Reports page. Admins will be able to generate and view various reports on user data and platform activity from here.</p>
      </CardContent>
    </Card>
  );
};

export default AdminReports; 